# Secrets — NEVER commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[oc]
.venv/
*.egg-info/

# Node / frontend
node_modules/
dist/
*.tsbuildinfo

# Databases (local state)
*.db
*.sqlite3

# Kaggle dataset dumps (62MB) — the committed backend/app/ml/_catalog_cache.json
# is derived from train.csv, so clones boot without re-downloading the data.
# Restore Data/train.csv locally to rebuild the catalog after changing it.
# Data/*.csv
# Model/submission.csv

# Logs
*.log
*.err

# OS / editor
Thumbs.db
.DS_Store

# Local docs / reference material — keep out of the repo
skill/
design.md
DESIGN_INSTRUCTION.md
instruction.md
memory.md

# Large ML cache artifact (local only)
backend/app/ml/_evidence_cache.pkl
